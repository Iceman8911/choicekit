---
title: Getting Started
description: Introduction to Choicekit
---

Inspired by [Twine SugarCube](https://www.motoslave.net/sugarcube/2/), Choicekit is a lightweight[^1], headless, framework-agnostic library for building interactive fiction.

---

## Features

- Passages and navigation
- State with snapshot-based history
- Saving and loading with versioning, optional compression, and migrations
- Deterministic pseudorandom number generation (PRNG)
- Custom class serialization for richer game objects
- Headless by design (you bring the renderer and UI)

---

# Installation

Install the published package `choicekit`.

import { Tabs, TabItem, Code } from "@astrojs/starlight/components"

<Tabs syncKey="packageManager">
  <TabItem label="npm">
    <Code code="npm install choicekit" lang="bash" />
  </TabItem>
  <TabItem label="pnpm">
    <Code code="pnpm add choicekit" lang="bash"   />
  </TabItem>
  <TabItem label="yarn">
    <Code code="yarn add choicekit" lang="bash"  />
  </TabItem>
  <TabItem label="bun">
    <Code code="bun add choicekit" lang="bash"  />
  </TabItem>
</Tabs>

## Sample usage

Notes on the current API:

- Prefer `ChoicekitEngineBuilder` for ergonomic type-safe setup
- Passage content lives on `passage.data`

```typescript
import { ChoicekitEngineBuilder } from "choicekit";

const engine = await new ChoicekitEngineBuilder()
	.withName("test-story")
	.withPassages(
		{ name: "Start", data: "You wake up in a quiet room.", tags: ["intro"] },
		{ name: "Hallway", data: "A long corridor stretches ahead." },
	)
	.withVars({ playerName: "Dave", inventory: { gold: 123, gems: 12 } })
	.build();

engine.setVars((state) => {
  state.playerName = "Sheep";
  state.inventory.gems = 21;
});

engine.navigateTo("Hallway");
```

[^1]: The full core is less than **10KB** after minification and gzip compression, however, with tree shaking, you'll likely get smaller sizes.
