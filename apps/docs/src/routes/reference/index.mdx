---
title: Reference
description: API reference generated from TypeDoc output
---

# Reference

This section is generated from the library’s TypeDoc JSON output under `apps/docs/src/generated/`.

- It is intended to **self-update** whenever you rebuild the library + regenerate TypeDoc.
- Pages under `/reference` should be treated as **derived output** (don’t hand-edit API details here—change JSDoc/type signatures in the library instead).

## What you’ll find here

- **Exports** (classes, functions, types) as emitted by TypeDoc
- **JSDoc summaries** and tags (when present)
- **Source links** (when available in the JSON)

## Navigation

- Browse the full API surface: [/reference/all](./all)
- Jump directly to an item by name: `/reference/symbol/<name>`

> If a symbol isn’t showing up, confirm it’s exported by the package entrypoint and that TypeDoc is including the relevant files.
