/**
 * Storylet config should take in an array of obejcts; each having an array of distiinct callbacks that resolve to booleans (with optional priorities), the passage to load if all callbacks resolve to true, and an optional priority number (defaulting to 0) that determines the order in which storylets are evaluated (higher priority storylets are evaluated first).
 */
