# `@packages/event-emitter`

Type-safe event emitter utilities for the repository.

Exports `TypedEventEmitter`, a small wrapper around `EventTarget` that uses an event map for strongly typed `on`, `once`, `off`, and `emit` calls.